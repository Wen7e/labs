#include <iostream>
#include <string>
using namespace std;


class LADA  
{                                          
protected:                   
	int power, mileage;   
};                            

class CHETIRKA : public LADA
{
public:
	int bodywork;

	string get_infoCHETIRKA() 
	{
		string info = "Мощность: " + to_string(power) + "\n" + "Пробег: " + to_string(mileage) + "\n" + "Хэтчбек: " + to_string(bodywork);
		return info;
	}

	void set_AI(int c, int m, int bdw) 
	{
		
		    mileage = m;                 
			power = c; 
			bodywork = bdw;
	}
};

class PYATNASHKA : public LADA 
{
public:
	int bodywork;

	string get_infoPYATNASHKA()
	{
		string info = "Мощность: " + to_string(power) + "\n" + "Пробег: " + to_string(mileage) + "\n" + "Хэтчбек: " + to_string(bodywork);
		return info;
	}

	void set_AI(int c, int m, int bdw)
	{
		power = c;
		mileage = m;
		bodywork = bdw;
	}
};

int main()
{
	setlocale(LC_ALL, "Russian");
	const int t = 6, n = 4;
	CHETIRKA chetirk[t];
	chetirk[0].set_AI(77, 65000, 1);
	chetirk[1].set_AI(82, 45900, 1);
	chetirk[2].set_AI(82, 135100, 1);
	chetirk[3].set_AI(77, 50700, 1);
	chetirk[4].set_AI(97, 00000, 1);
	chetirk[5].set_AI(82, 170000, 1);
	PYATNASHKA pyatnashk[n];
	pyatnashk[0].set_AI(97, 000, 0);
	pyatnashk[1].set_AI(77, 45600, 0);
	pyatnashk[2].set_AI(82, 17777, 0);
	pyatnashk[3].set_AI(97, 77777, 0);

	cout << "Четырок на складе: " << t - 1 << endl;
	cout << "Характеристики: \n";
	for (int i = 1; i < t; i++)
		cout << "Четырка №" << i << ": \n" << chetirk[i].get_infoCHETIRKA() << endl;

	cout << "Пятнашек на складе: " << n - 1 << endl;
	cout << "Характеристики: \n ";
	for (int i = 1; i < n; i++)
		cout << "Пятнашка №" << i << ": \n" << pyatnashk[i].get_infoPYATNASHKA() << endl;
	system("pause");
	return 0;
}
